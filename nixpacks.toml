[phases.setup]
nixPkgs = ["php83", "php83Packages.composer"]

[phases.build]
cmds = ["php -r \"dl_test: extension_loaded('pdo_mysql') ? print('ok') : print('no');\""]

[start]
cmd = "php -d extension=pdo_mysql -d extension=mysqli -S 0.0.0.0:$PORT"